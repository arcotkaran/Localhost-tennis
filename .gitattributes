# Normalize line endings: store and check out text files with LF so the repo
# stays consistent across Windows/macOS/Linux (and Git stops warning on commit).
* text=auto eol=lf
*.png binary
*.jpg binary
*.glb binary
